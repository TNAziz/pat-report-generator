"""PAT Report Generator — domain logic.

This package contains all data ingestion, normalization, analysis, and
rendering logic. No module under `pat/` is allowed to import Streamlit;
that boundary is enforced by `scripts/check_imports.py`.
"""

__version__ = "0.1.0a0"
