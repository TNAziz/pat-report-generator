__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
.env
.DS_Store
Thumbs.db
# Local cache directory if user overrides PAT_DATA_DIR into the project
data_cache/
# Leftover from app.py -> Home.py rename; OneDrive wouldn't let us delete it locally.
_app.py.bak
# Real PAT export CSVs — input data, not source.
PAT DATA/
# Session-to-session handoff notes; local scratch only.
HANDOFF.md
# Untracked working data and transfer artifacts, none of it source.
Fall2026/
_to_delete/
_transfer_pat-repo.tar.gz
.claude/
